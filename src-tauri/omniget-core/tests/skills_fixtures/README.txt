Fixtures for core/skills (f3-skills-core). Written for these tests, not copied
from anyone: each folder is a hand-made SKILL.md that exercises one branch of
the parser. `bad-*` and `no-*` folders are expected to fail.
